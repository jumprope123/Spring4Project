<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pds">
    <insert id="insertPds" statementType="PREPARED" parameterType="shin.spring.mvc.vo.PdsVO">
        insert into Pds (title, userid, contents, fname1, fname2, fname3, fsize1, fsize2, fsize3, ftype1, ftype2, ftype3)
        values (#{title}, #{userid}, #{contents}, #{fname1}, #{fname2}, #{fname3}, #{fsize1}, #{fsize2}, #{fsize3}, #{ftype1}, #{ftype2}, #{ftype3});
    </insert>


    <select id="selectList" statementType="PREPARED" resultType="shin.spring.mvc.vo.PdsVO" parameterType="int">
        select pno, title, userid, regdate, thumbs, views from Pds order by pno desc limit #{startnum}, 10;
    </select>

    <select id="countPds" statementType="PREPARED" resultType="int">
        select count(pno) cnt from Pds;
    </select>

    <select id="selectOne" statementType="PREPARED" resultType="shin.spring.mvc.vo.PdsVO" parameterType="String">
        select * from Pds where pno = #{pno};
    </select>

</mapper>